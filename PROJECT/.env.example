# Optional: use only if you run with Docker and want to override defaults.
# Copy to .env in this folder (project root).

# Backend port (used by docker-compose backend service)
# PORT=4000

# MongoDB (used if you override in docker-compose)
# MONGO_URI=mongodb://mongo:27017/brainbolt

# Redis (used if you override in docker-compose)
# REDIS_URL=redis://redis:6379
